# Signal + Channel Model — compatibility pointer

The durable current-system contract moved to [Signal + Channel Data Model](data-model/signal-channel-model.md).

The former M16 migration/audit narrative was removed from current documentation. Git history and the completed M16 Issues/PRs preserve the migration chronology.

New documentation should link directly to `data-model/signal-channel-model.md`.
